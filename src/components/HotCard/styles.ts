import styled from 'styled-components'

export const HNContainer = styled.div `

    display: flex;
    flex-direction: column;
    gap: 10px;
    border: 1px solid #cecece;
    border-radius: 8px;
    padding: 10px;
    margin: 5px;
    width: 600px;
    background-color: #cecece;

`

export const HNHeader = styled.section `

    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: baseline;

`

export const HNTitle = styled.h4 ``

export const HNSubtitle = styled.p ``

export const HNBody = styled.div `

    display: flex;
    width: 100%;
    justify-content: space-between;

`

export const HNButton = styled.a `

    border: none;
    outline: none;
    text-decoration: none;
    cursor: pointer;

`

export const HNPublishDate = styled.p `

    

`